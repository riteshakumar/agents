# Agent Template (Python)

Use this as a starting point for new agents.

## Run

```bash
python3 agent.py
```

## Customize

- Update the description at the top of `agent.py`.
- Add any new dependencies to your project config.
- Add cron helpers in `scripts/` if needed.
